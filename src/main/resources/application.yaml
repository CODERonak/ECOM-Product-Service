server:
  port: 9092

spring:
  application:
    name: product-service
# 
  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: PRODUCTS
      auto-index-creation: true

  security:
    user:
      name: boot
      password: spring  

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_URL}      


# logging:
#   level:
#     org:
#       springframework:
#         security: TRACE